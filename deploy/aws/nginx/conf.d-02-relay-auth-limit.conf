# Outer brake on sign-in endpoints: 10 requests/second per client with a small
# burst. The API applies the real per-account and per-IP lockouts behind this.
limit_req_zone $binary_remote_addr zone=relay_auth:10m rate=10r/s;
limit_req_status 429;
