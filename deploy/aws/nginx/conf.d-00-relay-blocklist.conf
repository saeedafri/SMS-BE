# Addresses blocked by hand, one per line:  deny 203.0.113.9;
# Apply with: sudo nginx -t && sudo systemctl reload nginx
# Automatic bans are fail2ban's (sudo fail2ban-client status relay-nginx-flood)
# and the API's (operator-admin bans).
