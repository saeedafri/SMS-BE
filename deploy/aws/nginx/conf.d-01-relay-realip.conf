# Traffic forwarded by the old Hostinger box during the DNS move carries the
# real caller in X-Forwarded-For; trust it only from that address.
set_real_ip_from 31.97.186.223;
real_ip_header X-Forwarded-For;
real_ip_recursive on;
