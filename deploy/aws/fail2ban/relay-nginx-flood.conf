# nginx refused an address for flooding (its own 429, so upstream_status is
# empty). Dashboard requests (bff 1) never count.
[Definition]
failregex = "client":"<HOST>",.*"status":429,.*"upstream_status":"",.*"bff":"0"\}$
datepattern = "time":"%%Y-%%m-%%dT%%H:%%M:%%S%%z"
